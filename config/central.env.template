#--------------------------------------------------------------------
# Template for config/central.env — copy this file and fill in values.
# central.env is gitignored; this .template is committed for reference.
#--------------------------------------------------------------------

CENTRAL_DB_HOST     = your-mysql-host.example.com
CENTRAL_DB_PORT     = 3306
CENTRAL_DB_USER     = nexfile_central_user
CENTRAL_DB_PASSWORD = CHANGE_ME
CENTRAL_DB_NAME     = nexfile_central

ADMIN_BE_URL = http://localhost:8087

# Generate with: php -r "echo bin2hex(random_bytes(32));"
TENANT_DB_ENCRYPTION_KEY = CHANGE_ME_64_HEX_CHARS
